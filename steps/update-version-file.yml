---
parameters:
  - name: version
    type: string

steps:
  - pwsh: |
      Write-Host "${{ parameters.version }}"
      "${{ parameters.version }}" | Out-File $(System.DefaultWorkingDirectory)/VERSION
    displayName: Update version file
