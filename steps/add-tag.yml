---
parameters:
  - name: force
    type: boolean
    default: false
  - name: tag
    type: string

steps:
  - ${{ if parameters.force }}:
      - script: |
          git tag -a "${{ parameters.tag }}" -m "${{ parameters.tag }}" --force
          git push --tags --force
        displayName: Add tag
  - ${{ else }}:
      - script: |
          git tag -a "${{ parameters.tag }}" -m "${{ parameters.tag }}"
          git push --tags
        displayName: Add tag
