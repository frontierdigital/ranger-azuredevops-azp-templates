---
parameters:
  - name: condition
    type: string
    default: always()

steps:
  - task: DeleteFiles@1
    inputs:
      contents: "**/*"
      sourceFolder: $(System.DefaultWorkingDirectory)
    displayName: Clear default working directory
    condition: ${{ parameters.condition }}
